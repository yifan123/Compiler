void test_expression() {
	int x, y, z, A[1];
	x = 10;
	y = 1;
	A[0 * 2] = 2;
	z = +x + (-1 * x*A[0] * (-y)*(+y) / 2 + '*' - 'A'); 
	if (z == 62) {
		printf("success!",z);
		return;
	}
	printf("fail!",z);
	return;
}

void main()
{
    test_expression();
}